server:
  port: 8082

spring:
  profiles:
    active: dev
  # 配置上传的最大文件大小 
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

#storage:
#  type: aliyun 