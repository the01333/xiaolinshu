spring:
  application:
    name: xiaolinshu-oss
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        namespace: xiaolinshu
      config:
        enabled: true
        server-addr: 127.0.0.1:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace: xiaolinshu
        file-extension: yaml
        refresh-enabled: true