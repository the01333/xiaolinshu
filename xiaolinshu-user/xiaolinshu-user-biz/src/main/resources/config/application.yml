server:
  port: 8083

spring:
  profiles:
    active: dev
  # 配置上传的最大文件大小 
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  